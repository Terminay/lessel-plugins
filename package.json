{
  "name": "lessel-plugins",
  "version": "1.0.0",
  "private": true,
  "description": "Community plugin registry for lessel",
  "scripts": {
    "build": "node scripts/build.js",
    "build:pages": "node scripts/build.js --pages"
  },
  "devDependencies": {}
}